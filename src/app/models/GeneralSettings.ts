namespace upk {
    export interface GeneralSettings {
        Id: string;
        CompanyName: string;
        LicenseKey: string;
        AutoLicenseUpdate: boolean;
        DBVersion: string;
        WebVersion: string;
        AppServerVersion: string;
    }
}